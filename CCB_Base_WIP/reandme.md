### changelog 文件格式

CCB在2.0版本之后将采用新的 changelog 格式。

请各位开发者在 `Changelog` 文件夹中建立自己的 changelog 文件，命名方式 `分支名.txt`。在每次编辑代码后，同步在自己的 changelog 文件中记录改动。大部分开发者只需要阅读 changelog 文件格式一节，后两节高级功能可以不读。

changelog 每一行作为一个句子，允许空行，`//` 后为注释。在开始改动之前，使用

`version 1.9.9.1`

来声明当前版本。例如

`哈瓦那：新增沿海惊艳额外+1文化值。`
